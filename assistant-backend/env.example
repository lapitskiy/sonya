# Rename to ".env" locally if you want, or export vars in your shell.
# Non-standard ports to avoid conflicts with other projects:
API_PORT=18000
POSTGRES_PORT=15432

# Worker poll interval
WORKER_POLL_SECONDS=2

